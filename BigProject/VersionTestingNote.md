## To do the A/B version testing:

To run the simulation with different set-ups of the adaptive intimacy and leader ties, run the `combo_param_runner.py` file in /running. 

It will take care of everything and the outputs will be in the output folder (which automatically gets created for you and will be under BigProject).
